{
  "name": "stoergeler-dev",
  "private": true,
  "scripts": {
    "dev:backend": "sh -c '. ./.venv/bin/activate && uvicorn backend.main:app --host 0.0.0.0 --port 8001'",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev": "sh -c '. ./.venv/bin/activate && uvicorn backend.main:app --host 0.0.0.0 --port 8001 & backend_pid=$!; npm --prefix frontend run dev; kill $backend_pid'"
  },
  "devDependencies": {
    "@semantic-release/commit-analyzer": "12.0.0",
    "@semantic-release/github": "11.0.1",
    "@semantic-release/release-notes-generator": "13.0.0",
    "conventional-changelog-angular": "5.0.13",
    "semantic-release": "^24.2.9"
  }
}
