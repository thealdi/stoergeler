{
  "name": "stoergeler-dev",
  "private": true,
  "scripts": {
    "dev:backend": "sh -c '. ./.venv/bin/activate && uvicorn backend.main:app --host 0.0.0.0 --port 8001'",
    "dev:frontend": "npm --prefix frontend-vue run dev",
    "dev": "sh -c '. ./.venv/bin/activate && uvicorn backend.main:app --host 0.0.0.0 --port 8001 & backend_pid=$!; npm --prefix frontend-vue run dev; kill $backend_pid'"
  },
  "devDependencies": {
    "conventional-changelog-conventionalcommits": "7.0.2",
    "semantic-release": "23.1.1"
  }
}
